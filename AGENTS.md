# Global Rules

## 基本ルール
- 日本語で回答する
- コマンドはセッションの実行環境（OS・シェル）に合わせる。macOS/zsh 前提で決め打ちしない

## 口調・回答スタイル
- 指摘を受けたときに決め台詞・褒め言葉から入らない（「いい指摘です」「確信をついています」等は書かない）。訂正や事実だけを述べてすぐ本題へ進む
- 回答の長さは質問の長さ・粒度に合わせる。短い質問には短く答え、詳細を求められて初めて長文で説明する

## PR・レビュー運用
- PR やレビューコメントへの返信・投稿は、内容を提示してユーザーの承認を得てから行う。勝手に返信しない
- PR 概要・コメントなどの公開される文面に Claude のセッション ID やセッション URL を書かない
- commit メッセージは本文とタイトルだけで完結させる。Claude のセッション ID・セッション URL・`Claude-Session:` / `Co-Authored-By: Claude` などのトレーラー、`Generated with Claude Code` 表記は一切入れない（PR 概要・コメントも同様）

## Pythonでのルール
1. パッケージ管理
   - `uv` のみを使用し、`pip` は絶対に使わない
   - インストール方法：`uv add package`
   - ツールの実行：`uv run tool`
   - アップグレード：`uv add --dev package --upgrade-package package`
   - 禁止事項：`uv pip install`、`@latest` 構文の使用

2. コード品質
   - すべてのコードに型ヒントを必須とする
   - 関数は集中して小さく保つこと
   - 既存のパターンを正確に踏襲すること
   - フォーマットと行長は `uv run ruff format` / `uv run ruff check` に従う（設定値はプロジェクトの pyproject.toml が正）

3. テスト要件
   - テストフレームワーク：`uv run --frozen pytest`
   - 非同期テストは `asyncio` ではなく `anyio` を使用
   - カバレッジはエッジケースやエラーも含めてテストすること
   - 新機能には必ずテストを追加すること

## docstring・コード内コメントのルール（全言語共通）
- プレーンな文章で書く。`**太字**`・見出し・箇条書きの多用など Markdown ファイルのような装飾をしない
- 「要件 2.4」のように外部ドキュメントの章番号を指す参照を書かない。そのドキュメントを開かないと意味が取れず、更新されると陳腐化する
- 根拠を示すときは関数名・ファイルパスなどコードから辿れるものを使う
- 量は周囲のコメントと同じ密度に抑える。1 コメント 1 論点で、Why を 1〜3 行で書いたら止める
- コメントに書かないもの: 環境ごとの現在の設定状態（dev は ON など）・日付・確認手順や運用メモ・他リポジトリの実装状況。これらは PR 本文に書く。コードには「なぜこの形か」だけを残す
